# URLs des microservices
currentaccount.url=http://localhost:8081/currentaccount
loan.url=http://localhost:8082/loan
deposit.url=http://localhost:8084

# Configuration EJB/JNDI
jndi.provider.url=http-remoting://localhost:8080
jndi.username=admin
jndi.password=admin

# Configuration du cache (en minutes)
cache.duration=5
cache.cleanup.interval=60

# Logging
logging.level.com.example.banking.central=INFO