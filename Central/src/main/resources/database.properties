# URLs des microservices
currentaccount.url=http://localhost:8081/currentaccount
loan.url=http://localhost:8082/loan
deposit.url=http://localhost:8084/depositaccount

# Configuration des timeouts (en millisecondes)
http.timeout.connection=5000
http.timeout.socket=10000

# Configuration du cache (en secondes)
cache.duration.compte=300
cache.duration.prets=600
cache.duration.depots=600

# Logging
logging.level.com.example.banking.central=INFO